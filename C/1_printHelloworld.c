#include <stdio.h> /* #include is a preprocessor command that tells the compiler to include the contents of a file
<stdio.h> : this file contains some useful function related to input and output; std= standard, i = input, o = output
*/

int main() { //the entry point of any function is the main function
//anything inside the main function is read procedure, from top to botton
//anything within the curly brackets are inside the main function
    printf("Hello, World!\n");

    return 0; //this returns the exit status of our programme, 0 if it runs successfully or 1 if not
}
